export declare function hasWhiteSpace(value: string): boolean;
export declare function isAlreadyQuoted(value: string): boolean;
export declare function isCommaSeparated(value: string): boolean;
export declare function escapeApostrophes(value: string): string;
export declare function processFontFamily(fontFamily: string | undefined): string;
/**
 * Helper: Transforms typography object to typography shorthand for CSS
 * This currently works fine if every value uses an alias, but if any one of these use a raw value, it will not be transformed.
 * If you'd like to output all typography values, you'd rather need to return the typography properties itself
 */
export declare function transformTypographyForCSS(value: Record<string, string | undefined | number> | undefined | string): string | undefined;
